##主配置信息
spring:
  profiles:
    active: dev,mysql,redis,actuator,sentinel
logging:
  config: classpath:config/logback-spring.xml
  level:
    io.swagger.models.parameters.AbstractSerializableParameter: error
